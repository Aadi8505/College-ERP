services:
  - type: web
    name: college-erp-backend
    runtime: node
    buildCommand: npm install && cd backend && npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
